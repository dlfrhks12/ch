<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cartMapper">
	
	<insert id="insert">
		<selectKey keyProperty="oNo" resultType="int" order="BEFORE">
			select orders_seq.nextval from dual
		</selectKey>
		insert into orders(o_no, u_username, m_name, o_total, o_order_time, s_num)
		values(#{oNo}, #{uUsername}, #{mName}, #{oTotal}, sysdate, #{sNum})
	</insert>
	
	<select id="findAll">
		select o_no oNo, u_username uUsername, m_name mName, o_total oTotal, o_order_time orderTime, s_num sNum
		from orders
	</select>
</mapper>












