<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="pointMapper">
	<select id="count" resultType="int">
		select count(*) from point where u_username=#{uUsername}
	</select>
	<select id="findAll" resultType="point">
	<![CDATA[
	select * from
		(select rownum as rnum, p.* from
			(select /*+ index_desc(point point_pk_u_username)*/ u_username uUsername, o_no oNo, accumulation_sal accumulationSal from point where u_username=#{uUsername}) p
		where rownum <=#{endRowNum})
	where rnum>=#{startRowNum}
	]]>	
	</select>
	<select id="findByTotalpoint" resultType="point">
		select total_point totalPoint from point where u_username=#{uUsername}
	</select>
	<insert id="insert">
		<selectKey keyProperty="uUsername" resultType="int" order="BEFORE">
			select point_seq.nextval from dual
		</selectKey>
		insert into point(u_username, o_no , accumulation_sal )
		values(#{uUsername}, #{oNo}, #{accumulationSal})
	</insert>	
	<update id="update">
		update point
		<trim suffixOverrides="," prefix="set">
			<if test="oNO=!null">o_no=#{oNO},</if>
			<if test="accumulationSal=!null">accumulation_sal=#{accumulationSal}</if>
		</trim>
		where u_username=#{UUsername} and rownum=1
	</update>
	<update id="pointAdd" parameterType="point">
		update member set point=point+#{Point} where u_username=#{uUsername}
	</update>
	<update id="pointMin" parameterType="point">
		update member set point=point-#{Point} where u_username=#{uUsername}
	</update>
	<delete id="delete">
		delete from point where u_username=#{uUsername} and rownum=1
	</delete>
</mapper>
